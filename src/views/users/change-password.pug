extends ../base

block content 
    form(method="POST")
        input(placeholder="Old Password", name="password", type="password")
        input(placeholder="New Password", name="password", type="password")
        input(placeholder="New Password Confirmation", name="password", type="password")
        input(type="submit", value="Change Password")